{
  "version": 2,
  "builds": [
    {
      "src": "packages/user/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/packages/user/dist/$1"
    }
  ]
}